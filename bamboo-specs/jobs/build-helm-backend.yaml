#other: !include runners/helm-xsmall.yaml
tasks:
  - script: |
      #!/bin/bash
      # Setup variables and call helm chart publish task.
      #export BASE_HELM_CHARTS_PATH="./backend/deploy"
      #export PACKAGES="components-api"
      #export APPLICATION="helm/feaas-components-api"
      #export REGISTRY=contenthub.azurecr.io
      #export REGISTRY_USER=${bamboo.mregistry.username.readwrite}
      #export REGISTRY_PASSWORD=${bamboo.mregistry.password.readwrite}
      #export CM_REGISTRY_USERNAME=${bamboo.charts.username}
      #export CM_REGISTRY_PASSWORD=${bamboo.charts.password}
      #export CHART_VERSION_NBR=${bamboo.build.version}
      #export CM_CHARTS_SERVER_URL=${bamboo.charts.url}
      #publish-charts
      echo "build-helm-backend"
