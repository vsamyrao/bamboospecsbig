#other: !include runners/docker-xsmall-dind-large.yaml
tasks:
  - script: |
      #!/bin/bash
      #source ${bamboo.scripts.path}/ci-init.sh
      #source ${bamboo.scripts.path}/ci-docker-compose-run-tests.sh backend-performance-tests
      echo "inside script"
final-tasks:
  - !include final-tasks/parse-junit-reports.yaml
